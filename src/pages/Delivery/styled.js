import styled from 'styled-components';
import { ContactTitle } from '../Contato/styled';

export const DeliveryText = styled(ContactTitle)`
  font-size: 30px;
  margin-bottom: 0px;
  font-weight: normal;
`;
